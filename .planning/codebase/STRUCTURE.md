# Structure
- `src/` frontend pages/components/hooks.
- `server/` backend routes, orchestrator, services, db schema.
- `shared/` shared type contracts.
- `tests/` currently minimal; needs route/integration expansion.
- `docs/` roadmap/blueprint/process docs (added in this revision).
