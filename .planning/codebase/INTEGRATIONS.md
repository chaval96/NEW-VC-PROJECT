# Integrations
- PostgreSQL via `DATABASE_URL`.
- Optional email provider (`RESEND_API_KEY`).
- Optional Playwright browser automation.
- Optional OpenAI research enrichment.
- Deployment targets include Railway/Fly style container deploy.
